{
  "name": "shadowsocks",
  "description": "a tunnel proxy that help you get through firewalls",
  "keywords": [
    "shadowsocks",
    "proxy",
    "socks5"
  ],
  "author": "clowwindy",
  "version": "2.0.1",
  "license": "MIT",
  "main": "build/server.js",
  "bin": {
    "ssserver": "src/bin/ss-server"
  },
  "scripts": {
    "test": "tsc && node build/encryptorTest",
    "server": "tsc && node build/server",
    "serverDaemon": "tsc && node build/serverDaemon",
    "local": "tsc && node  build/local"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/qieru/ss-js"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "futoin-hkdf": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^12.11.1"
  }
}
